CREATE TABLE B AS
SELECT * FROM A WHERE quoteid IN (
    SELECT quoteid
    FROM A
    GROUP BY quoteid
    HAVING COUNT(DISTINCT sysd) = 1 AND MAX(sysd) = 'Y'
)
UNION
SELECT * FROM A WHERE quoteid IN (
    SELECT quoteid
    FROM A
    GROUP BY quoteid
    HAVING COUNT(DISTINCT sysd) = 1
);
