# Databricks notebook source
# MAGIC %md
# MAGIC # Guidewire ODS — Data Reconciliation (Read Replica TST vs CDA)
# MAGIC Standalone from the DDL/schema-drift script by design — separate metadata
# MAGIC queries, separate output table, safe to schedule as its own Job and run
# MAGIC **in parallel** with the DDL reconciliation without either interfering
# MAGIC with the other.
# MAGIC
# MAGIC Scope: **row-level data only**. Schema/DDL drift is handled by
# MAGIC `gw_ddl_schema_drift_reconciliation.py`. This script trusts that a table
# MAGIC pair is worth comparing and focuses purely on whether the data in it
# MAGIC matches.
# MAGIC
# MAGIC Sources:
# MAGIC - **Read Replica TST** — `core_tst.ods_views` (baseline for data compare)
# MAGIC - **CDA** — `core_tst_std001.ods_views`, same table name + `_cda` suffix
# MAGIC
# MAGIC Consistent with the DDL script: any table in `core_tst` ending in the CDA
# MAGIC suffix is a stray artifact, not a genuine Read Replica table, and is
# MAGIC excluded from discovery entirely.

# COMMAND ----------
import json
import logging
import sys
import threading
import time
import uuid
from collections import Counter, OrderedDict
from concurrent.futures import ThreadPoolExecutor, as_completed
from contextlib import contextmanager
from dataclasses import dataclass, field
from datetime import datetime, timedelta
from typing import Dict, List, Optional, Tuple

from pyspark.sql import DataFrame, functions as F
from pyspark.sql.window import Window
from pyspark.sql.types import (
    StructType, StructField, StringType, LongType, DoubleType, BooleanType, TimestampType
)

# COMMAND ----------
# MAGIC %md ## 1. Configuration

# COMMAND ----------
@dataclass
class DataReconConfig:
    # -- Sources --------------------------------------------------------------
    source_catalog: str = "core_tst"            # Read Replica TST (baseline for data)
    target_catalog: str = "core_tst_std001"      # CDA
    schema_name: str = "ods_views"
    target_table_suffix: str = "_cda"

    domain_prefixes: Dict[str, str] = field(default_factory=lambda: {
        "policycenter": "vw_policycenter_current_p",
        "billingcenter": "vw_billingcenter_current_b",
        "claimcenter": "vw_claimcenter_current_c",
    })

    # -- Column handling --------------------------------------------------------
    # Pipeline/audit columns expected to differ -- excluded from data compare.
    audit_columns: List[str] = field(default_factory=lambda: [
        "azure_load_date", "dwh_current_flag", "dwh_from_date",
        "dwh_until_date", "beanversion", "archivepartition",
    ])
    # CDC metadata column that only exists in CDA -- fully ignored.
    cda_only_columns: List[str] = field(default_factory=lambda: ["gwcbi___operation"])

    # Timestamp columns to search for (priority order) for the incremental filter.
    timestamp_filter_priority: List[str] = field(default_factory=lambda: ["updatetime", "createtime"])

    # Candidate unique-key columns, in priority order, for join-based
    # column-level mismatch diagnosis. First one present on both sides wins.
    key_columns_priority: List[str] = field(default_factory=lambda: ["id", "publicid", "externalid"])

    # -- Incremental filter window ------------------------------------------------
    lookback_days: int = 7
    filter_start_date: Optional[str] = None
    filter_end_date: Optional[str] = None

    # -- Sampling (row counts routinely diverge: TST refreshes daily, CDA on demand)
    sample_size: int = 50_000
    sample_fraction_cap: float = 0.20
    min_sample_size: int = 1_000
    full_compare_row_limit: int = 20_000_000
    large_table_sample_size: int = 500_000

    # When a table has NEITHER updatetime NOR createtime (so no incremental
    # filter can be applied), the full-vs-sample decision switches from
    # "do counts match" to a straight row-count threshold: small tables get
    # a full key-based compare regardless of count equality; larger tables
    # get sampled instead of risking a full unfiltered scan/join.
    no_timestamp_full_compare_row_threshold: int = 5_000

    # -- Column-level diagnostics --------------------------------------------------
    max_example_rows_per_column: int = 3
    max_mismatched_columns_in_comment: int = 15

    # Split each column mismatch into VALUE (genuinely different data) vs
    # FORMAT (same value, different representation -- case, whitespace,
    # trailing zeros) so formatting noise doesn't read as a real defect.
    detect_format_mismatches: bool = True

    # -- Output ---------------------------------------------------------------------
    output_catalog: str = "core_tst_std001"
    output_schema: str = "testing_sb"
    output_table: str = "gw_data_reconciliation_summary"
    write_results: bool = True
    results_mode: str = "append"

    # -- Resilience -------------------------------------------------------------------
    continue_on_error: bool = True
    max_table_retries: int = 1
    fail_run_if_error_rate_above: Optional[float] = None

    # -- Performance --------------------------------------------------------------------
    # Compares run concurrently across tables via a Python thread pool -- each
    # worker submits its own DataFrame API calls against the shared
    # SparkSession, which is the standard/supported pattern for concurrent
    # Spark jobs (no RDD API, no SparkContext manipulation -- serverless-safe).
    # This is the single biggest lever at 2K-table scale: with the previous
    # fully-sequential loop, per-table Spark job-scheduling overhead alone
    # (a handful of small jobs x ~1-2s scheduling latency each) dominates
    # wall-clock time long before any actual data volume does. Tune down if
    # you see the cluster saturated / other jobs starved; tune up if the
    # cluster has spare capacity and tables are mostly small.
    max_workers: int = 8

    def get_filter_window(self) -> Tuple[str, str]:
        if self.filter_start_date and self.filter_end_date:
            return self.filter_start_date, self.filter_end_date
        end = datetime.utcnow().date()
        start = end - timedelta(days=self.lookback_days)
        return start.isoformat(), (end + timedelta(days=1)).isoformat()

    def source_schema_fqn(self) -> str:
        return f"{self.source_catalog}.{self.schema_name}"

    def target_schema_fqn(self) -> str:
        return f"{self.target_catalog}.{self.schema_name}"

    def output_table_fqn(self) -> str:
        return f"{self.output_catalog}.{self.output_schema}.{self.output_table}"

    def excluded_from_compare(self) -> set:
        return {c.lower() for c in self.audit_columns} | {c.lower() for c in self.cda_only_columns}


# COMMAND ----------
# MAGIC %md ## 2. Logging & run metrics

# COMMAND ----------
def get_logger(name: str = "gw_data_reconciliation") -> logging.Logger:
    logger = logging.getLogger(name)
    if logger.handlers:
        return logger
    logger.setLevel(logging.INFO)
    handler = logging.StreamHandler(sys.stdout)
    handler.setFormatter(logging.Formatter(
        fmt="%(asctime)s | %(levelname)-8s | %(name)s | %(message)s", datefmt="%Y-%m-%d %H:%M:%S"))
    logger.addHandler(handler)
    logger.propagate = False
    return logger


logger = get_logger()


class RunMetrics:
    def __init__(self):
        self.run_id = str(uuid.uuid4())
        self.started_at = datetime.utcnow()
        self.finished_at = None
        self.status_counts = Counter()
        self.table_durations = {}
        self.errors = {}
        self._lock = threading.Lock()   # guards the mutable state above across worker threads

    def record_status(self, table_name: str, status: str):
        with self._lock:
            self.status_counts[status] += 1

    def record_duration(self, table_name: str, seconds: float):
        with self._lock:
            self.table_durations[table_name] = seconds

    def record_error(self, table_name: str, err: Exception):
        with self._lock:
            self.errors[table_name] = repr(err)

    def finalize(self):
        self.finished_at = datetime.utcnow()

    def error_rate(self) -> float:
        total = sum(self.status_counts.values())
        return 0.0 if total == 0 else self.status_counts.get("ERROR", 0) / total

    def print_summary(self):
        logger.info("=" * 70)
        logger.info("DATA RECONCILIATION RUN SUMMARY")
        logger.info(json.dumps({
            "run_id": self.run_id,
            "started_at": self.started_at.isoformat(),
            "finished_at": self.finished_at.isoformat() if self.finished_at else None,
            "total_tables": sum(self.status_counts.values()),
            "status_breakdown": dict(self.status_counts),
            "error_count": len(self.errors),
            "errors": self.errors,
        }, indent=2, default=str))
        logger.info("=" * 70)


@contextmanager
def timed(metrics: RunMetrics, table_name: str):
    start = time.time()
    try:
        yield
    finally:
        metrics.record_duration(table_name, round(time.time() - start, 2))


# COMMAND ----------
# MAGIC %md ## 3. Table discovery — bulk metadata, not per-table lookups
# MAGIC Same approach as the DDL script: one `information_schema.columns` query
# MAGIC per catalog, filtered by the naming pattern, instead of touching each
# MAGIC table individually. This both discovers the table pairs *and* gives us
# MAGIC the comparable-column list for free, so no separate schema pass is
# MAGIC needed before the data compare.

# COMMAND ----------
def fetch_catalog_columns(spark, catalog: str, schema: str, prefixes: List[str]) -> Dict[str, "OrderedDict[str, str]"]:
    """Returns { table_name_lower: OrderedDict(column_name_lower -> data_type) }."""
    prefix_conditions = " OR ".join(f"lower(table_name) LIKE '{p.lower()}%'" for p in prefixes)
    query = f"""
        SELECT lower(table_name) AS table_name, lower(column_name) AS column_name, data_type, ordinal_position
        FROM {catalog}.information_schema.columns
        WHERE lower(table_schema) = lower('{schema}')
          AND ({prefix_conditions})
        ORDER BY table_name, ordinal_position
    """
    try:
        rows = spark.sql(query).collect()
    except Exception as e:
        logger.error(f"Metadata fetch failed for {catalog}.{schema}: {e!r}")
        return {}

    result: Dict[str, "OrderedDict[str, str]"] = {}
    for r in rows:
        result.setdefault(r["table_name"], OrderedDict())[r["column_name"]] = r["data_type"]
    logger.info(f"{catalog}.{schema}: fetched metadata for {len(result)} table(s)")
    return result


@dataclass
class TablePair:
    domain: str
    source_table: str
    target_table: str
    source_fqn: str
    target_fqn: str
    comparable_columns: List[str]     # common columns, minus audit/CDC exclusions
    columns_only_in_source: List[str]
    columns_only_in_target: List[str]


def discover_table_pairs(spark, config: DataReconConfig) -> Tuple[List[TablePair], List[str]]:
    """
    Returns (pairs_ready_to_compare, missing_in_target_table_names).
    Tables in the source catalog ending in the CDA suffix are stray
    artifacts (Read Replica should never carry that naming) and are
    excluded from discovery entirely, matching the DDL script's behaviour.
    """
    prefixes = list(config.domain_prefixes.values())
    source_meta = fetch_catalog_columns(spark, config.source_catalog, config.schema_name, prefixes)
    target_meta = fetch_catalog_columns(spark, config.target_catalog, config.schema_name, prefixes)

    excluded = config.excluded_from_compare()
    pairs: List[TablePair] = []
    missing_in_target: List[str] = []

    for domain, prefix in config.domain_prefixes.items():
        prefix_lc = prefix.lower()
        source_tables = sorted(
            t for t in source_meta
            if t.startswith(prefix_lc) and not t.endswith(config.target_table_suffix)
        )
        stray = sorted(t for t in source_meta if t.startswith(prefix_lc) and t.endswith(config.target_table_suffix))
        if stray:
            logger.warning(f"Domain '{domain}': {len(stray)} stray _cda-suffixed table(s) in "
                           f"{config.source_catalog} -- excluded from discovery: {stray}")

        for src_table in source_tables:
            tgt_table = f"{src_table}{config.target_table_suffix}"
            if tgt_table not in target_meta:
                missing_in_target.append(src_table)
                continue

            src_cols = set(source_meta[src_table]) - excluded
            tgt_cols = set(target_meta[tgt_table]) - excluded
            pairs.append(TablePair(
                domain=domain, source_table=src_table, target_table=tgt_table,
                source_fqn=f"{config.source_schema_fqn()}.{src_table}",
                target_fqn=f"{config.target_schema_fqn()}.{tgt_table}",
                comparable_columns=sorted(src_cols & tgt_cols),
                columns_only_in_source=sorted(src_cols - tgt_cols),
                columns_only_in_target=sorted(tgt_cols - src_cols),
            ))

    return pairs, missing_in_target


# COMMAND ----------
# MAGIC %md ## 4. Data comparison — key-based, column-level diagnostics
# MAGIC
# MAGIC 1. Filter both sides on `UPDATETIME`/`CREATETIME` + configured window, if present.
# MAGIC 2. Pick a join key (`id` → `publicid` → `externalid`).
# MAGIC 3. **Timestamp column present** → full compare when row counts match, sample
# MAGIC    compare when they don't (expected: TST refreshes daily, CDA on demand).
# MAGIC 4. **No timestamp column at all** → decided purely by table size: ≤5,000 rows
# MAGIC    gets a full key-based compare regardless of count equality; >5,000 rows
# MAGIC    gets sampled, to avoid an unfiltered full-table scan/join.
# MAGIC 5. **No usable key column** → falls back to a full-row hash diff (tells you
# MAGIC    rows differ, not which column).
# MAGIC 6. Every column mismatch is tagged **VALUE** (genuinely different data) or
# MAGIC    **FORMAT** (same value once trimmed/lower-cased/zero-stripped -- e.g.
# MAGIC    `'Active'` vs `'active'`, `'123.50'` vs `'123.5'`) so formatting noise
# MAGIC    doesn't get triaged as a real data defect.

# COMMAND ----------
def _normalize_for_format_check(col):
    """Trim + lowercase + collapse whitespace + strip trailing zeros on plain
    decimals, so 'Active'/'active' and '123.50'/'123.5' are recognized as the
    same value in a different format rather than a genuine mismatch."""
    s = F.regexp_replace(F.trim(F.lower(col)), r"\s+", " ")
    is_plain_decimal = s.rlike(r"^-?\d+\.\d+$")
    zero_stripped = F.regexp_replace(F.regexp_replace(s, r"0+$", ""), r"\.$", "")
    return F.when(is_plain_decimal, zero_stripped).otherwise(s)


@dataclass
class ColumnMismatch:
    column_name: str
    mismatch_count: int
    value_mismatch_count: int = 0
    format_mismatch_count: int = 0
    examples: List[Tuple[str, str, str, str]] = field(default_factory=list)  # (key, source_value, target_value, mismatch_type)


@dataclass
class DataComparisonResult:
    source_row_count: int = 0
    target_row_count: int = 0
    comparison_mode: str = "NONE"          # FULL_KEYED | SAMPLE_KEYED | FULL_HASH | SKIPPED
    filter_applied: bool = False
    filter_column: Optional[str] = None
    filter_window: Optional[Tuple[str, str]] = None
    key_column: Optional[str] = None
    matched_row_count: int = 0
    rows_only_in_source: int = 0
    rows_only_in_target: int = 0
    sample_size: Optional[int] = None
    column_mismatches: List[ColumnMismatch] = field(default_factory=list)
    hash_mismatch_count: Optional[int] = None

    def has_value_mismatch(self) -> bool:
        """A real data defect: missing/extra rows, or a genuine VALUE-level
        column mismatch (not just formatting)."""
        if self.comparison_mode == "FULL_HASH":
            return bool(self.hash_mismatch_count)
        if self.rows_only_in_source or self.rows_only_in_target:
            return True
        return any(cm.value_mismatch_count > 0 for cm in self.column_mismatches)

    def has_format_only_variance(self) -> bool:
        """Every observed difference is FORMAT (same value, different
        representation) with zero genuine VALUE mismatches -- non-blocking."""
        if self.comparison_mode == "FULL_HASH" or self.has_value_mismatch():
            return False
        return any(cm.format_mismatch_count > 0 for cm in self.column_mismatches)

    def status(self) -> str:
        if self.comparison_mode == "SKIPPED":
            return "NOT_COMPARED"
        if self.has_value_mismatch():
            return "DATA_MISMATCH"
        if self.has_format_only_variance():
            return "FORMAT_VARIANCE"
        return "DATA_MATCH"


def _pick_filter_column(df: DataFrame, config: DataReconConfig) -> Optional[str]:
    cols = {c.lower() for c in df.columns}
    for candidate in config.timestamp_filter_priority:
        if candidate in cols:
            return candidate
    return None


def _resolve_actual_col(df: DataFrame, lower_name: str) -> str:
    return next((c for c in df.columns if c.lower() == lower_name), lower_name)


def _apply_filter(df: DataFrame, filter_col: Optional[str], window: Tuple[str, str]) -> DataFrame:
    if not filter_col:
        return df
    start, end = window
    actual_col = _resolve_actual_col(df, filter_col)
    return df.where((F.col(actual_col) >= F.lit(start)) & (F.col(actual_col) < F.lit(end)))


def _pick_key_column(source_df: DataFrame, target_df: DataFrame, comparable_cols: List[str],
                      config: DataReconConfig) -> Optional[str]:
    src_cols = {c.lower() for c in source_df.columns}
    tgt_cols = {c.lower() for c in target_df.columns}
    for candidate in config.key_columns_priority:
        if candidate in comparable_cols and candidate in src_cols and candidate in tgt_cols:
            return candidate
    return None


def _mismatches_from_matched(matched_df: DataFrame, compare_cols: List[str], key_alias: str,
                              config: DataReconConfig) -> List[ColumnMismatch]:
    """
    Given a dataframe already joined on the key (columns prefixed s__/t__),
    computes per-column mismatch counts + examples in one pass. Callers are
    expected to have already cached matched_df.
    """
    if not compare_cols:
        return []

    def _mismatch_struct(c: str):
        s_col, t_col = F.col(f"s__{c}"), F.col(f"t__{c}")
        # Compare on the string-cast representation, not the raw typed
        # columns. TST and CDA can (and do) disagree on a column's
        # underlying Spark type -- comparing raw types directly can throw
        # DATATYPE_MISMATCH.BINARY_OP_DIFF_TYPES (e.g. one side binary, the
        # other string) and abort the whole table. Casting both sides to
        # string first is universally comparable and still catches genuine
        # value differences; type drift itself is the DDL script's job.
        s_str, t_str = s_col.cast("string"), t_col.cast("string")
        raw_mismatch = ~s_str.eqNullSafe(t_str)
        if config.detect_format_mismatches:
            s_norm, t_norm = _normalize_for_format_check(s_str), _normalize_for_format_check(t_str)
            is_format = raw_mismatch & s_str.isNotNull() & t_str.isNotNull() & s_norm.eqNullSafe(t_norm)
            mismatch_type = F.when(is_format, F.lit("FORMAT")).otherwise(F.lit("VALUE"))
        else:
            mismatch_type = F.lit("VALUE")
        return F.when(
            raw_mismatch,
            F.struct(
                F.lit(c).alias("column_name"),
                s_str.alias("source_value"),
                t_str.alias("target_value"),
                mismatch_type.alias("mismatch_type"),
            ),
        )

    mismatch_struct_exprs = [_mismatch_struct(c) for c in compare_cols]
    exploded = (
        matched_df.withColumn("__mismatches__", F.filter(F.array(*mismatch_struct_exprs), lambda x: x.isNotNull()))
        .where(F.size("__mismatches__") > 0)
        .select(key_alias, F.explode("__mismatches__").alias("m"))
        .select(
            F.col(key_alias).cast("string").alias("key"),
            F.col("m.column_name").alias("column_name"),
            F.col("m.source_value").alias("source_value"),
            F.col("m.target_value").alias("target_value"),
            F.col("m.mismatch_type").alias("mismatch_type"),
        )
    ).cache()

    try:
        count_rows = (
            exploded.groupBy("column_name")
            .agg(
                F.count("*").alias("total_cnt"),
                F.sum(F.when(F.col("mismatch_type") == "VALUE", 1).otherwise(0)).alias("value_cnt"),
                F.sum(F.when(F.col("mismatch_type") == "FORMAT", 1).otherwise(0)).alias("format_cnt"),
            )
            .collect()
        )
        counts = {r["column_name"]: (r["total_cnt"], r["value_cnt"], r["format_cnt"]) for r in count_rows}
        if not counts:
            return []

        w = Window.partitionBy("column_name").orderBy(F.monotonically_increasing_id())
        example_rows = (
            exploded.withColumn("rn", F.row_number().over(w))
            .where(F.col("rn") <= config.max_example_rows_per_column)
            .select("column_name", "key", "source_value", "target_value", "mismatch_type")
            .collect()
        )
        examples_by_col: Dict[str, List[Tuple[str, str, str, str]]] = {}
        for r in example_rows:
            examples_by_col.setdefault(r["column_name"], []).append(
                (r["key"], r["source_value"], r["target_value"], r["mismatch_type"])
            )

        return [
            ColumnMismatch(
                column_name=col, mismatch_count=total, value_mismatch_count=val_cnt, format_mismatch_count=fmt_cnt,
                examples=examples_by_col.get(col, []),
            )
            for col, (total, val_cnt, fmt_cnt) in sorted(counts.items(), key=lambda kv: -kv[1][0])
        ]
    finally:
        exploded.unpersist()


def _full_keyed_compare(source_df: DataFrame, target_df: DataFrame, key_col: str,
                         compare_cols: List[str], config: DataReconConfig) -> Tuple[int, int, int, List[ColumnMismatch]]:
    """
    ONE cached full-outer join answers all three questions (rows only in
    source, rows only in target, matched-row mismatches) instead of the
    previous approach of two separate exceptAll anti-joins plus a third,
    independent inner join -- i.e. one physical scan/shuffle of each table
    instead of three.
    """
    key_src, key_tgt = _resolve_actual_col(source_df, key_col), _resolve_actual_col(target_df, key_col)
    s_sel = [F.col(key_src).alias("s__key__")] + [F.col(_resolve_actual_col(source_df, c)).alias(f"s__{c}") for c in compare_cols]
    t_sel = [F.col(key_tgt).alias("t__key__")] + [F.col(_resolve_actual_col(target_df, c)).alias(f"t__{c}") for c in compare_cols]

    joined = (
        source_df.select(*s_sel)
        .join(target_df.select(*t_sel), F.col("s__key__") == F.col("t__key__"), "full_outer")
        .cache()
    )
    try:
        rows_only_in_source = joined.where(F.col("t__key__").isNull()).count()
        rows_only_in_target = joined.where(F.col("s__key__").isNull()).count()
        matched_df = joined.where(F.col("s__key__").isNotNull() & F.col("t__key__").isNotNull()).cache()
        try:
            matched_count = matched_df.count()
            mismatches = _mismatches_from_matched(matched_df, compare_cols, "s__key__", config) if matched_count else []
        finally:
            matched_df.unpersist()
        return rows_only_in_source, rows_only_in_target, matched_count, mismatches
    finally:
        joined.unpersist()


def _sample_keyed_compare(source_df: DataFrame, target_df: DataFrame, key_col: str, compare_cols: List[str],
                           sample_n: int, source_count: int, config: DataReconConfig) -> Tuple[int, int, List[ColumnMismatch]]:
    """
    ONE cached left-outer join (sampled source keys -> target) answers both
    "how many sampled keys are missing in CDA" and the column-level
    mismatch diagnostics, instead of a separate anti-join plus a separate
    inner join as before.
    """
    key_src, key_tgt = _resolve_actual_col(source_df, key_col), _resolve_actual_col(target_df, key_col)
    fraction = min(1.0, sample_n / source_count * 1.5)
    sampled_keys = (source_df.select(key_src).distinct()
                    .sample(withReplacement=False, fraction=fraction, seed=42)
                    .limit(sample_n)).cache()
    try:
        sampled_source = source_df.join(sampled_keys, on=key_src, how="inner")
        s_sel = [F.col(key_src).alias("s__key__")] + [F.col(_resolve_actual_col(sampled_source, c)).alias(f"s__{c}") for c in compare_cols]
        t_sel = [F.col(key_tgt).alias("t__key__")] + [F.col(_resolve_actual_col(target_df, c)).alias(f"t__{c}") for c in compare_cols]

        joined = (
            sampled_source.select(*s_sel)
            .join(target_df.select(*t_sel), F.col("s__key__") == F.col("t__key__"), "left_outer")
            .cache()
        )
        try:
            rows_only_in_source = joined.where(F.col("t__key__").isNull()).count()
            matched_df = joined.where(F.col("t__key__").isNotNull()).cache()
            try:
                matched_count = matched_df.count()
                mismatches = _mismatches_from_matched(matched_df, compare_cols, "s__key__", config) if matched_count else []
            finally:
                matched_df.unpersist()
            return rows_only_in_source, matched_count, mismatches
        finally:
            joined.unpersist()
    finally:
        sampled_keys.unpersist()


def _hash_df(df: DataFrame, columns: List[str]) -> DataFrame:
    actual_cols = [_resolve_actual_col(df, c) for c in columns]
    exprs = [F.coalesce(F.col(c).cast("string"), F.lit("\u0000NULL\u0000")) for c in sorted(actual_cols)]
    return df.select(F.sha2(F.concat_ws("||", *exprs), 256).alias("__row_hash__"))


def compare_data(spark, pair: TablePair, config: DataReconConfig) -> DataComparisonResult:
    result = DataComparisonResult()
    comparable_cols = pair.comparable_columns

    source_df_raw = spark.table(pair.source_fqn)
    target_df_raw = spark.table(pair.target_fqn)

    filter_col = _pick_filter_column(source_df_raw, config)
    window = config.get_filter_window()
    if filter_col:
        result.filter_applied, result.filter_column, result.filter_window = True, filter_col, window
        source_df = _apply_filter(source_df_raw, filter_col, window)
        target_df = _apply_filter(target_df_raw, filter_col, window)
    else:
        source_df, target_df = source_df_raw, target_df_raw

    if not comparable_cols:
        result.comparison_mode = "SKIPPED"
        return result

    # Cache the (filtered) frames once -- every downstream count/join in this
    # function reuses this same materialization instead of re-reading and
    # re-filtering the underlying table from scratch each time.
    source_df, target_df = source_df.cache(), target_df.cache()
    try:
        source_count, target_count = source_df.count(), target_df.count()
        result.source_row_count, result.target_row_count = source_count, target_count

        key_col = _pick_key_column(source_df, target_df, comparable_cols, config)
        result.key_column = key_col
        non_key_compare_cols = [c for c in comparable_cols if c != key_col]

        if key_col is None:
            result.comparison_mode = "FULL_HASH"
            src_hash, tgt_hash = _hash_df(source_df, comparable_cols), _hash_df(target_df, comparable_cols)
            only_source, only_target = src_hash.exceptAll(tgt_hash).count(), tgt_hash.exceptAll(src_hash).count()
            result.rows_only_in_source, result.rows_only_in_target = only_source, only_target
            result.hash_mismatch_count = only_source + only_target
            return result

        if source_count == 0 and target_count == 0:
            result.comparison_mode = "FULL_KEYED"
            return result

        # Exactly one side is empty -> nothing meaningful to sample; every
        # row on the non-empty side has no counterpart on the other side.
        if source_count == 0 or target_count == 0:
            result.comparison_mode = "FULL_KEYED"
            result.rows_only_in_source = source_count
            result.rows_only_in_target = target_count
            return result

        # Full-vs-sample decision:
        #  - Timestamp filter available -> use it as normal: full compare
        #    when the (already-filtered) counts match, sample when they don't.
        #  - No timestamp column at all -> can't narrow the window, so decide
        #    by absolute size instead: small tables (<= threshold) get a full
        #    key-based compare regardless of count equality; larger tables
        #    get sampled rather than risking an unfiltered full scan/join.
        if filter_col:
            do_full = (source_count == target_count) and (max(source_count, target_count) <= config.full_compare_row_limit)
        else:
            do_full = max(source_count, target_count) <= config.no_timestamp_full_compare_row_threshold

        if do_full:
            result.comparison_mode = "FULL_KEYED"
            (result.rows_only_in_source, result.rows_only_in_target,
             result.matched_row_count, result.column_mismatches) = _full_keyed_compare(
                source_df, target_df, key_col, non_key_compare_cols, config
            )
            return result

        # Row counts differ -> representative sample from Read Replica TST
        result.comparison_mode = "SAMPLE_KEYED"
        smaller_count = min(source_count, target_count) or 1
        sample_n = min(
            config.large_table_sample_size if max(source_count, target_count) > config.full_compare_row_limit else config.sample_size,
            max(config.min_sample_size, int(smaller_count * config.sample_fraction_cap)),
        )
        sample_n = max(1, min(sample_n, source_count))
        result.sample_size = sample_n

        (result.rows_only_in_source, result.matched_row_count, result.column_mismatches) = _sample_keyed_compare(
            source_df, target_df, key_col, non_key_compare_cols, sample_n, source_count, config
        )
        return result
    finally:
        source_df.unpersist()
        target_df.unpersist()


# COMMAND ----------
# MAGIC %md ## 5. Comment builder

# COMMAND ----------
def _fmt_list(items) -> str:
    return ", ".join(str(i) for i in items) if items else ""


def _fmt_column_mismatches(mismatches: List[ColumnMismatch], config: DataReconConfig, key_col: str) -> str:
    if not mismatches:
        return ""
    shown = mismatches[: config.max_mismatched_columns_in_comment]
    parts = []
    for cm in shown:
        breakdown = f"{cm.value_mismatch_count} VALUE, {cm.format_mismatch_count} FORMAT" if config.detect_format_mismatches else ""
        breakdown_txt = f" [{breakdown}]" if breakdown else ""
        example_strs = [f"{key_col}={k}: TST='{s}' vs CDA='{t}' [{mtype}]" for k, s, t, mtype in cm.examples]
        examples_txt = f" (e.g. {'; '.join(example_strs)})" if example_strs else ""
        parts.append(f"{cm.column_name.upper()}: {cm.mismatch_count} row(s) differ{breakdown_txt}{examples_txt}")
    text = "COLUMN MISMATCHES -> " + "; ".join(parts) + "."
    remaining = len(mismatches) - len(shown)
    if remaining > 0:
        text += f" (+{remaining} more mismatched column(s) not shown)."
    return text


def _build_comments(pair: TablePair, data_result: Optional[DataComparisonResult], table_status: str,
                     config: DataReconConfig) -> str:
    if table_status == "MISSING_IN_TARGET":
        return f"Table exists in Read Replica TST but was not found in CDA (expected name: {pair.source_table}_cda)."

    parts = []
    if pair.columns_only_in_source:
        parts.append(f"{len(pair.columns_only_in_source)} column(s) in TST not comparable (absent in CDA, or excluded): "
                     f"{_fmt_list(pair.columns_only_in_source)}.")
    if pair.columns_only_in_target:
        parts.append(f"{len(pair.columns_only_in_target)} column(s) in CDA not comparable (absent in TST, or excluded): "
                     f"{_fmt_list(pair.columns_only_in_target)}.")

    if data_result:
        if data_result.comparison_mode == "SKIPPED":
            parts.append("No comparable common columns after exclusions -- data comparison skipped.")
            return " ".join(parts)

        if data_result.source_row_count != data_result.target_row_count:
            parts.append(f"Row count mismatch (expected in this env): TST={data_result.source_row_count}, "
                         f"CDA={data_result.target_row_count}.")

        if data_result.comparison_mode == "FULL_HASH":
            parts.append("No key column (id/publicid/externalid) found -- fell back to full-row hash compare; "
                         "column-level diagnostics unavailable for this table.")
            if data_result.rows_only_in_source or data_result.rows_only_in_target:
                parts.append(f"{data_result.rows_only_in_source} row-hash(es) only in TST, "
                             f"{data_result.rows_only_in_target} only in CDA.")
            else:
                parts.append("All row hashes match.")
        else:
            mode_label = "Full" if data_result.comparison_mode == "FULL_KEYED" else f"Sample ({data_result.sample_size} keys)"
            key_label = data_result.key_column.upper() if data_result.key_column else "N/A (empty table)"
            parts.append(f"{mode_label} key-based compare on {key_label}: "
                         f"{data_result.matched_row_count} matched row(s) checked.")
            if data_result.rows_only_in_source:
                parts.append(f"{data_result.rows_only_in_source} key(s) present in TST but not found in CDA.")
            if data_result.comparison_mode == "FULL_KEYED" and data_result.rows_only_in_target:
                parts.append(f"{data_result.rows_only_in_target} key(s) present in CDA but not found in TST.")
            if data_result.column_mismatches:
                parts.append(_fmt_column_mismatches(data_result.column_mismatches, config, data_result.key_column or "key"))
            elif data_result.matched_row_count:
                parts.append("All compared columns match on matched rows.")

        if data_result.filter_applied and data_result.filter_column:
            parts.append(f"Incremental filter on {data_result.filter_column.upper()} window {data_result.filter_window}.")
        else:
            parts.append("No UPDATETIME/CREATETIME column found -- full table compared, no date filter.")

    return " ".join(parts) if parts else "No issues detected."


def _overall_status(table_status: str, data_status: str) -> str:
    if table_status in ("MISSING_IN_TARGET", "ERROR"):
        return "FAIL" if table_status == "MISSING_IN_TARGET" else "ERROR"
    if data_status == "DATA_MISMATCH":
        return "FAIL"
    if data_status in ("NOT_COMPARED", "FORMAT_VARIANCE"):
        return "WARN"
    return "PASS"


# COMMAND ----------
# MAGIC %md ## 6. Orchestrator

# COMMAND ----------
RESULT_SCHEMA = StructType([
    StructField("run_id", StringType()),
    StructField("run_timestamp", TimestampType()),
    StructField("domain", StringType()),
    StructField("source_table", StringType()),
    StructField("target_table", StringType()),
    StructField("table_status", StringType()),
    StructField("data_status", StringType()),
    StructField("overall_status", StringType()),
    StructField("source_row_count", LongType()),
    StructField("target_row_count", LongType()),
    StructField("comparison_mode", StringType()),
    StructField("key_column", StringType()),
    StructField("filter_applied", BooleanType()),
    StructField("filter_column", StringType()),
    StructField("matched_row_count", LongType()),
    StructField("rows_only_in_source", LongType()),
    StructField("rows_only_in_target", LongType()),
    StructField("sample_size", LongType()),
    StructField("mismatched_columns", StringType()),
    StructField("mismatched_column_count", LongType()),
    StructField("value_mismatch_columns", StringType()),        # columns with >=1 genuine VALUE mismatch -- the real issues
    StructField("format_only_mismatch_columns", StringType()),  # columns where every mismatch was FORMAT-only (no VALUE) -- lower priority
    StructField("duration_seconds", DoubleType()),
    StructField("comments", StringType()),
])


def _reconcile_one(spark, pair: TablePair, config: DataReconConfig, run_id: str, run_ts) -> dict:
    start = time.time()
    data_result = compare_data(spark, pair, config)
    data_status = data_result.status()
    overall = _overall_status("OK", data_status)
    mismatched_col_names = [cm.column_name for cm in data_result.column_mismatches]
    value_issue_cols = [cm.column_name for cm in data_result.column_mismatches if cm.value_mismatch_count > 0]
    format_only_cols = [cm.column_name for cm in data_result.column_mismatches
                        if cm.value_mismatch_count == 0 and cm.format_mismatch_count > 0]

    return dict(
        run_id=run_id, run_timestamp=run_ts, domain=pair.domain,
        source_table=pair.source_table, target_table=pair.target_table,
        table_status="OK", data_status=data_status, overall_status=overall,
        source_row_count=data_result.source_row_count, target_row_count=data_result.target_row_count,
        comparison_mode=data_result.comparison_mode, key_column=data_result.key_column,
        filter_applied=data_result.filter_applied, filter_column=data_result.filter_column,
        matched_row_count=data_result.matched_row_count,
        rows_only_in_source=data_result.rows_only_in_source, rows_only_in_target=data_result.rows_only_in_target,
        sample_size=data_result.sample_size,
        mismatched_columns=_fmt_list(mismatched_col_names), mismatched_column_count=len(mismatched_col_names),
        value_mismatch_columns=_fmt_list(value_issue_cols),
        format_only_mismatch_columns=_fmt_list(format_only_cols),
        duration_seconds=round(time.time() - start, 2),
        comments=_build_comments(pair, data_result, "OK", config),
    )


def run_data_reconciliation(spark, config: DataReconConfig):
    run_id = str(uuid.uuid4())
    run_ts = datetime.utcnow()
    metrics = RunMetrics()
    metrics.run_id = run_id

    logger.info(f"Starting data reconciliation run {run_id}")
    logger.info(f"Source (TST): {config.source_schema_fqn()} | Target (CDA): {config.target_schema_fqn()}")

    pairs, missing_in_target = discover_table_pairs(spark, config)
    logger.info(f"Discovered {len(pairs)} comparable table pair(s); {len(missing_in_target)} missing in CDA")
    logger.info(f"Running with max_workers={config.max_workers} (concurrent table comparisons)")

    results: List[dict] = []

    for src_table in missing_in_target:
        results.append(dict(
            run_id=run_id, run_timestamp=run_ts, domain=None, source_table=src_table,
            target_table=f"{src_table}_cda", table_status="MISSING_IN_TARGET", data_status="NOT_COMPARED",
            overall_status="FAIL", source_row_count=None, target_row_count=None, comparison_mode="SKIPPED",
            key_column=None, filter_applied=False, filter_column=None, matched_row_count=None,
            rows_only_in_source=None, rows_only_in_target=None, sample_size=None,
            mismatched_columns=None, mismatched_column_count=None,
            value_mismatch_columns=None, format_only_mismatch_columns=None, duration_seconds=None,
            comments=f"Table exists in Read Replica TST but was not found in CDA (expected name: {src_table}_cda).",
        ))
        metrics.record_status(src_table, "FAIL")

    def _process_one_pair(pair) -> dict:
        table_label = f"{pair.domain}.{pair.source_table}"
        attempts = 0
        while True:
            attempts += 1
            try:
                with timed(metrics, table_label):
                    row = _reconcile_one(spark, pair, config, run_id, run_ts)
                metrics.record_status(table_label, row["overall_status"])
                logger.info(f"[{row['overall_status']}] {table_label} (data={row['data_status']}, "
                            f"src_rows={row['source_row_count']}, tgt_rows={row['target_row_count']}, "
                            f"mismatched_cols={row['mismatched_columns']})")
                return row
            except Exception as e:
                if attempts <= config.max_table_retries:
                    logger.warning(f"Retrying {table_label} after error: {e!r}")
                    continue
                logger.error(f"[ERROR] {table_label} failed after {attempts} attempt(s): {e!r}")
                metrics.record_error(table_label, e)
                metrics.record_status(table_label, "ERROR")
                if not config.continue_on_error:
                    raise
                return dict(
                    run_id=run_id, run_timestamp=run_ts, domain=pair.domain,
                    source_table=pair.source_table, target_table=pair.target_table,
                    table_status="ERROR", data_status="NOT_COMPARED", overall_status="ERROR",
                    source_row_count=None, target_row_count=None, comparison_mode="NONE", key_column=None,
                    filter_applied=False, filter_column=None, matched_row_count=None,
                    rows_only_in_source=None, rows_only_in_target=None, sample_size=None,
                    mismatched_columns=None, mismatched_column_count=None,
                    value_mismatch_columns=None, format_only_mismatch_columns=None, duration_seconds=None,
                    comments=f"Reconciliation failed: {e!r}",
                )

    # Tables are independent of each other, so they're compared concurrently
    # via a thread pool instead of one at a time. Each worker thread submits
    # its own DataFrame API calls against the shared SparkSession (standard,
    # serverless-safe pattern -- no RDD API, no SparkContext manipulation).
    with ThreadPoolExecutor(max_workers=max(1, config.max_workers)) as executor:
        futures = {executor.submit(_process_one_pair, pair): pair for pair in pairs}
        for future in as_completed(futures):
            pair = futures[future]
            try:
                results.append(future.result())
            except Exception as e:
                # Only reachable when continue_on_error is False -- surface
                # immediately rather than waiting for every other thread.
                logger.error(f"Aborting run: {pair.domain}.{pair.source_table} failed and continue_on_error=False")
                raise

    metrics.finalize()
    metrics.print_summary()

    total = sum(metrics.status_counts.values())
    if config.fail_run_if_error_rate_above is not None and metrics.error_rate() > config.fail_run_if_error_rate_above:
        raise RuntimeError(f"Data reconciliation error rate {metrics.error_rate():.1%} exceeded threshold "
                            f"{config.fail_run_if_error_rate_above:.1%}")

    summary_df = spark.createDataFrame(results, schema=RESULT_SCHEMA)

    if config.write_results:
        target = config.output_table_fqn()
        try:
            spark.sql(f"CREATE SCHEMA IF NOT EXISTS {config.output_catalog}.{config.output_schema}")
            (summary_df.write.format("delta").mode(config.results_mode)
                .option("mergeSchema", "true").saveAsTable(target))
            logger.info(f"Data reconciliation results written to {target} ({config.results_mode} mode)")
        except Exception as e:
            logger.error(f"Failed to persist results to {target}: {e!r}")

    return summary_df


# COMMAND ----------
# MAGIC %md ## 7. Run
# MAGIC Independent widgets/parameters from the DDL job -- schedule this as its
# MAGIC own Databricks Job task and it can run concurrently with the DDL check.
# MAGIC
# MAGIC `filter_start_date`/`filter_end_date` take precedence when both are set
# MAGIC (format `YYYY-MM-DD`); leave either blank to fall back to `lookback_days`.

# COMMAND ----------
dbutils.widgets.text("source_catalog", "core_tst")
dbutils.widgets.text("target_catalog", "core_tst_std001")
dbutils.widgets.text("filter_start_date", "")     # e.g. 2026-07-01 -- blank = use lookback_days
dbutils.widgets.text("filter_end_date", "")       # e.g. 2026-07-13 -- blank = use lookback_days
dbutils.widgets.text("lookback_days", "7")
dbutils.widgets.text("sample_size", "50000")
dbutils.widgets.text("max_workers", "8")
dbutils.widgets.dropdown("write_results", "true", ["true", "false"])

# COMMAND ----------
_start = dbutils.widgets.get("filter_start_date").strip() or None
_end = dbutils.widgets.get("filter_end_date").strip() or None

config = DataReconConfig(
    source_catalog=dbutils.widgets.get("source_catalog"),
    target_catalog=dbutils.widgets.get("target_catalog"),
    filter_start_date=_start,
    filter_end_date=_end,
    lookback_days=int(dbutils.widgets.get("lookback_days")),
    sample_size=int(dbutils.widgets.get("sample_size")),
    max_workers=int(dbutils.widgets.get("max_workers")),
    write_results=(dbutils.widgets.get("write_results") == "true"),
)

summary_df = run_data_reconciliation(spark, config)
display(summary_df)

# COMMAND ----------
# MAGIC %md ## 8. Triage view
# MAGIC `value_mismatch_columns` = real issues to act on. `format_only_mismatch_columns`
# MAGIC = same value, different representation -- worth a glance, not a defect.

# COMMAND ----------
issues = summary_df.where(F.col("overall_status").isin("FAIL", "WARN", "ERROR")).orderBy(
    F.col("overall_status").desc(), F.col("mismatched_column_count").desc_nulls_last()
)
print(f"{issues.count()} table(s) flagged out of {summary_df.count()} compared.")
display(issues.select(
    "domain", "source_table", "overall_status", "data_status", "comparison_mode",
    "source_row_count", "target_row_count",
    "value_mismatch_columns", "format_only_mismatch_columns", "comments"
))

# Uncomment to hard-fail the job on any FAIL/ERROR (FORMAT_VARIANCE-only tables stay WARN, not blocking):
# hard_failures = issues.where(F.col("overall_status").isin("FAIL", "ERROR")).count()
# if hard_failures:
#     raise Exception(f"Data reconciliation found {hard_failures} failing table(s). See summary table for details.")
