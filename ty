# Filter out columns with empty values
columns_with_data = [col for col in csv_df.columns if not csv_df.filter(csv_df[col].isNotNull()).isEmpty()]

# Create a new DataFrame with only non-empty columns
filtered_csv_df = csv_df.select(*columns_with_data)
