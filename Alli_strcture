SELECT 
    table_name,
    CASE 
        WHEN LOWER(LEFT(table_name, 1)) = 'p' THEN 'PC'
        WHEN LOWER(LEFT(table_name, 1)) = 'b' THEN 'BC'
        WHEN LOWER(LEFT(table_name, 1)) = 'c' THEN 'CC'
        ELSE 'UNKNOWN'
    END AS domain
FROM your_table;
