steps:
- task: UsePythonVersion@0
  inputs:
    versionSpec: '3.8'

- script: |
    databricks fs cp dbfs:/tmp/test-results.xml test-results.xml
  displayName: 'Copy test result XML from DBFS'

- task: PublishTestResults@2
  inputs:
    testResultsFiles: 'test-results.xml'
    testRunTitle: 'Databricks Pytest Results'
