Hi [BA Name],

My understanding of the `myLicence.entitlements.validfrom` logic is that it is not storing the actual licence valid-from date directly. Instead, the requirement calculates the number of completed months between the entitlement `validFrom` date and the `policy_EffectiveStartDate`, and derives the value based on that duration.

Because the requirement specifically refers to “number of completed months”, could you please confirm whether the target datatype for this field should actually be an Integer (months value) rather than a Date field?

Just wanted to confirm the expected output format from the business perspective.
