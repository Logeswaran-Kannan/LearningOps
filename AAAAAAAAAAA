.withColumn(
    "quote_status",
    F.when(
        F.datediff(
            F.to_date("eHubQueuedDatestamp"),
            F.to_date("quoteDate")
        ) > 7,
        "invalidDatesince"
    ).otherwise("validDate")
)

(Already safe — Spark handles nulls)

✍️ One-line reviewer reply

Updated logic to use date diffe
