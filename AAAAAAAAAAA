import socket
try:
    s = socket.create_connection(("ap-pric-npd-uks-evhn-001.servicebus.windows.net", 9093), timeout=10)
    print("✅ Connection OK")
    s.close()
except Exception as e:
    print("❌ Connection FAILED:", e)




EHubConnectionString = dbutils.secrets.get(
    scope="Azure-secret-key-vault",
    key="pts-mqs-bicoe-eventhub"
)
print("Length:", len(EHubConnectionString))
print("Starts correctly:", EHubConnectionString.startswith("Endpoint=sb://"))
