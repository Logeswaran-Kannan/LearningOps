Yes, the target table will be created automatically on the first run using saveAsTable. For production, the TARGET_TABLE config will be updated from the build schema to the approved production schema, so the table is created in the correct location with the expected Delta format and partitioning.
