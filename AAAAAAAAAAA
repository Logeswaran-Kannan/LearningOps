Hi Team,

My interpretation of the DVLA endorsement derivation logic is below, and I would like to confirm whether our understanding is correct before implementation and validation.

Regarding the conviction code lookup logic, our understanding is that the final derived `myLicence.endorsements.code` is determined based on:

1. Original conviction code
2. Ban duration category

Our assumption is that the ban duration category is derived from `myLicence.endorsements.disqualperiod`.

Example interpretation:

* Conviction Code = AC20
* Disqualification Period = P2M
* Ban category interpreted as “Ban 1-3”
* Derived Conviction Code = AC10

Similarly:

* Conviction Code = CD70
* Disqualification Period = P2Y
* Ban category interpreted as “Ban 13+”
* Derived Conviction Code = CD40

However, from the JSON payloads currently available to us, we are unable to identify a separate raw “ban duration” field directly in the structure. Our assumption is that the lookup table expects the ban category to be interpreted from the `disqualperiod` value itself.

Could you please confirm if this interpretation is correct?

Secondly, the document mentions:

“The MyLicence Disqualification Period is input in ISO8601 duration format”

with example:
`P1Y3M1DT12M`

However, in the JSON payloads currently available to us, the value already appears to be arriving in the derived/bucketed format itself (for example `P2M`, `P6M`, `P2Y`) rather than the raw ISO8601 duration structure. We are also unable to identify any separate raw field containing the original ISO8601 duration value.

From our current understanding, it appears the value may already be processed even in the raw message itself.

Could you please confirm whether:

* the incoming JSON value should simply be treated as the final derived value and processed as-is, or
* whether an upstream raw ISO8601 duration field is expected to be available anywhere in the raw request/message structure?

Thanks,
