test_payload = {
    "text": "Radar Live test message"
}

response = requests.post(
    WEBHOOK_URL.strip(),
    headers={"Content-Type": "application/json"},
    data=json.dumps(test_payload),
    timeout=30
)

print(response.status_code)
print(response.text)
