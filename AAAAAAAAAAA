Just a small clarification on the current scope. At this stage, we already know there are schema and data differences between the Read Replica and CDA, so there isn't a need to validate those as part of this activity.

The current objective is specifically to validate the **CDC logic** using a small set of sample policies.

Could you please select a few sample policy records and verify the following:

* **Insert:** A newly created policy is reflected as an INSERT event in CDA.
* **Update:** Changes to a policy generate the expected UPDATE event and the SCD version/history is maintained correctly.
* **Delete:** A deleted or cancelled policy is represented correctly based on the Guidewire CDC design (delete event or delete indicator, as applicable).
* Verify that the CDC events are generated in the correct sequence with the expected timestamps.
* Confirm there are no missing or duplicate CDC events for the selected policy samples.

The purpose of this validation is to confirm that CDA is correctly deriving CDC events from the underlying SCD history for insert, update, and delete scenarios.
