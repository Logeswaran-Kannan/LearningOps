CREATE TABLE IF NOT EXISTS your_table_name (
  name STRING,
  size BIGINT,
  path STRING,
  creation_date TIMESTAMP,
  last_update_date TIMESTAMP,
  del_status STRING,
  del_date STRING
)
USING delta;
