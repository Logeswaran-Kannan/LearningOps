CREATE TABLE your_table_name (
    name VARCHAR(255),
    size BIGINT,
    path VARCHAR(255) PRIMARY KEY,
    creation_date DATE,
    last_update_date DATE,
    deletable BOOLEAN,
    del_date DATE
);
